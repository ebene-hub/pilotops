node_modules/
dist/
*.local
.DS_Store
.vite/
.env
!.env.example
supabase/volumes/
supabase/.temp/
supabase/.branches/
seed-pilot.mjs
GGIS-UAV-Companion-debug.apk
*.apk

# SSH private keys — never commit (incl. editor/backup copies)
*.pem
*.key
*.pem.*
*.key.*

# Android release signing keystore — never commit
*.jks
*.keystore
